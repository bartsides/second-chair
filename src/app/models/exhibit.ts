export interface Exhibit {
  id: string;
  trialId: string;
  defendant: boolean;
  marker: string;
  description: string;
  notes: string;
  supportingWitness: string;
  admittanceStoplight: string;
}
